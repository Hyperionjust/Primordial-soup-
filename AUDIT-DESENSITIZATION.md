# 脱敏终检报告（AUDIT-DESENSITIZATION）

- 审计对象：`primordial-soup/` 发布包全目录（spec / scripts / examples / adapters / README / LICENSE / 本文档）
- 审计方法：固定字符串全文件扫描（Select-String -SimpleMatch），UTF-8 无 BOM 检查，脚本编译检查 + 合成样例冒烟
- 结论：**全部红线模式 0 命中，可发布**
- 说明：本文档为元文档，按发布门禁要求不出现任何红线模式的字面量，检查项以描述方式列出（实际扫描用原始字面量执行）。

## 一、必查模式扫描结果（全部 0 命中）

| 检查项（描述） | 命中数 |
|---|---|
| 私有工作区根路径前缀（盘符级路径段） | 0 |
| 私有项目目录名 | 0 |
| 私有库目录名的路径用法（目录名＋分隔符） | 0 |
| 宿主专属技能目录名（点开头） | 0 |
| 私有移植包代号 | 0 |
| 私人项目代号 | 0 |
| 裁定者旧代号 | 0 |
| 私人评估代号 / 测试代号 / 私有卡名 / 角色代号 | 0 |
| 用户目录路径段 / 私人仓库路径段 | 0 |

保留且合法：机制名「原始汤」（纯文字用法，非路径）；宿主名 Claude / DSH；组件名 soup-memory / soup-tide-weekly（技能与潮汐任务名）；合成卡名（测试甲等，任务明确允许）。

## 二、清洗记录（列出问题 → 清洗 → 重跑至 0）

1. **scripts\ 四脚本**（原样复制后清洗，仅注释/字符串，零行为改动，py_compile 4/4 PASS）：
   - `闪电抽样器.py`：裁定者旧代号 拍板→用户拍板；注释「DSH 移植改造版（私有包代号）」→「通用改造版」；README 引用句→「仓库 README」；「私有库原版」→「原版」（5 处）。
   - `状态页生成器.py`：同上私有包代号/私有库原版；宿主专属技能目录扫描改为通用约定 `.agents/skills`（保持「统计汤根内技能目录数」语义，样例无该目录、输出不变）；artifact 与宿主专属注释中性化（3 处）。
   - `M5结算脚本.py`：「通用改造版」；评估代号三原则句删除，改为判据确定性表述。
   - `潮汐周报生成器.py`：裁定者旧代号→用户（4 处，含生成 HTML 文案），并修「裁定在 用户」空格。
   - 注：首轮清洗时脚本数组扁平化误伤 M5/潮汐两文件（字符级替换），已从源文件恢复重洗；以 diff 逐行核对最终版与源文件仅存在上述注释级差异；`py -m py_compile` 4/4 通过；对 `examples/mini-soup` 临时副本实跑四脚本全部 EXIT=0（抽样器 seed 复现、状态页 --stamp fixed、M5 --sampler、潮汐对账 19/19）。
2. **examples\mini-soup\**（样例汤，原样复制后清洗）：`闪电台账.md`（列头与注释 2 处）、`潮汐周报\潮汐-2026-08-17.md`（1 处）、`状态页.html`（内嵌 RAW 2 处）——「旧代号裁定」→「用户裁定」、「待旧代号裁定」→「待用户裁定」，与清洗后的台账/规范一致。
3. **spec\SOUP-MECHANISM.md**：由 DSH 版 SKILL.md 清洗重写——删 frontmatter 与「DSH 移植注」「与 Claude 版差异」节；裁定者旧代号全量→用户（文首加「本规范统一以『用户』指代机制裁定者」）；私有库内部文件引用（机制清单、外部机制借鉴提案等）改为「机制清单随维护仓库管理，公开版不包含」或删句；bash/python3/挂载路径→命令行（Windows 用 pwsh，解释器探测 py→python→python3）、AskUserQuestion→交互确认工具（一题一卡、四档）、Cowork artifact 仪表盘→生成静态 HTML 状态页、soup-tide-weekly 定时任务→每周一次的潮汐对账任务（调度方式见部署文档）；机制规则本体（范围守卫/文件夹契约/编号/标签两维/闪电步骤/台账句式/沉淀/哨兵/结案快道/维护）逐字保留。
4. **spec\lightning-trigger.md**：由触发点设计文档清洗——去 DSH 专属术语（ask_user_question→交互确认工具、schedule_create→定时机制、goal→任务），去检出路径取证句；T1-T6 条款、防重判据、边界清单保留。
5. **adapters\ds-harness\SKILL.md**：DSH 版全文（保留全部 DSH 专属机制细节），清洗红线——裁定者旧代号全量→用户；私有包路径占位符→`<适配包路径>`；宿主专属技能目录路径→「私有部署库中的原版」；私有库内部文件引用中性化；私人评估/测试代号、私有卡名删除或中性化；frontmatter 未动（YAML 引号完好）。
6. **adapters\ds-harness\INSTALL.md**：按原安装说明重写——删除私有路径示例与私有库红线句，命令示例改为 `<适配包路径>`/`<汤根绝对路径>` 占位符；DSH 安装细节（安放位/热重载/分发两模式/参数速查/冒烟指引）保留。
7. **adapters\ds-harness\PORTING-NOTES.md**：移植清单 + 设计笔记合并稿，全程规避红线——取证文件路径改中性描述；私有库源路径→「私有部署库」；角色代号不出现；发现 1-21 与工单 R1-R7 技术内容保留。
8. **README.md / examples\GETTING-STARTED.md**：新写；「从哪来」用中性句「本项目机制脚本经私有部署环境测试后开源」。

## 三、BOM 与编码

- 全包文件 UTF-8 无 BOM 扫描：**0 个带 BOM**（样例汤 `状态页.html` 亦为 UTF-8 无 BOM）。

## 四、发布前验证

- `py -m py_compile`：四脚本 4/4 通过。
- 合成样例冒烟（临时副本，未触碰包内文件）：抽样器 EXIT=0（同 seed 复现、权重表正常、同族去重生效）；状态页生成器 EXIT=0 产出 HTML；M5 `--sampler` EXIT=0（指标与台账一致、口径提示行在）；潮汐周报生成器 EXIT=0（对账 19/19 落页）。
- LICENSE：Apache-2.0 标准全文（取自本机 node_modules 纯净副本），版权行 `Copyright 2026 primordial-soup contributors`。

## 五、声明

本审计覆盖本仓库全部文件；发布前若再改动任何文件，须重跑第一节扫描（用原始字面量，勿用本文档的描述性写法）。
